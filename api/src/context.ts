import { join } from "path";

export const workspaceDir = join(__dirname, "../workspace");
export const mainListDir = join(workspaceDir, "main");
export const secondListDir = join(workspaceDir, "second");
export const finalListDir = join(workspaceDir, "final");
export const mainTempDir = join(workspaceDir, "temp");
